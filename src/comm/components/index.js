import cpButton from './Button';
import cpStatusBar from './StatusBar';
import cpText from './Text';

export const Button = cpButton;

export const StatusBar = cpStatusBar;

export const Text = cpText;

export default {
  Button,
  StatusBar,
  Text,
};
